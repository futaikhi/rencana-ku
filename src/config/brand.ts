/**
 * RencanaKu Brand & Logo Configuration (For Developers)
 *
 * As a developer, you can customize the logo here:
 * 1. Place your logo file in the /public directory (e.g., /public/logo.png or /public/favicon.ico)
 * 2. Set the path in the logoUrl property below (e.g., "/logo.png")
 * 3. If logoUrl is null or file is not found, the app automatically falls back to
 *    the authentic RencanaKu Bento SVG emblem.
 */
export const BRAND_CONFIG = {
  appName: "RencanaKu",
  tagline: "Bento Planning OS",
  // Replace with your custom PNG / ICO logo path (e.g., "/logo.png", "/my-logo.png", etc.)
  // Set to null to use the default Bento SVG emblem
  logoUrl: "/logo.png" as string | null,
  faviconUrl: "/favicon.ico",
};
